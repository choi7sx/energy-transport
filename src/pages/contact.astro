---
import { getEntry } from "astro:content";
import ManagedContact from "../components/pages/managed-contact.astro";

const page = await getEntry("pages", "contact");
if (!page) throw new Error("Missing managed contact page content");
---

<ManagedContact page={page.data} />
