---
import { getEntry } from "astro:content";
import ManagedDetail from "../components/pages/managed-detail.astro";

const page = await getEntry("pages", "products");
if (!page) throw new Error("Missing managed products page content");
---

<ManagedDetail page={page.data} />
