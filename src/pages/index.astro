---
import { getEntry } from "astro:content";
import ManagedHome from "../components/pages/managed-home.astro";

const page = await getEntry("pages", "index");
if (!page) throw new Error("Missing managed home page content");
---

<ManagedHome page={page.data} />
