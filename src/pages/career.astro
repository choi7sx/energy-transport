---
import { getEntry } from "astro:content";
import ManagedCareer from "../components/pages/managed-career.astro";

const page = await getEntry("pages", "career");
if (!page) throw new Error("Missing managed career page content");
---

<ManagedCareer page={page.data} />
