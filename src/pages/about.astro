---
import { getEntry } from "astro:content";
import ManagedAbout from "../components/pages/managed-about.astro";

const page = await getEntry("pages", "about");
if (!page) throw new Error("Missing managed about page content");
---

<ManagedAbout page={page.data} />
