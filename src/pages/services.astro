---
import { getEntry } from "astro:content";
import ManagedDetail from "../components/pages/managed-detail.astro";

const page = await getEntry("pages", "services");
if (!page) throw new Error("Missing managed services page content");
---

<ManagedDetail page={page.data} />
